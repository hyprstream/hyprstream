# Optional isolated resolver fixture. It listens only on an unprivileged local
# port and does not alter the host resolver. Query with:
#   dig @127.0.0.1 -p 53535 alice.accounts.localhost
no-resolv
no-hosts
bind-interfaces
listen-address=127.0.0.1
port=53535
address=/accounts.localhost/127.0.0.1
