# Generated by `cargo run -p hyprstream-k8s --bin gen-crds`.
# Do not edit by hand; edit the Rust types in crates/hyprstream-k8s/src/.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: trainingruns.training.hyprstream.io
spec:
  group: training.hyprstream.io
  names:
    categories:
    - hyprstream
    kind: TrainingRun
    plural: trainingruns
    shortNames:
    - hstrain
    singular: trainingrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelRef
      name: Model
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A TTT / adapter training job intent. spec is desired intent; status is observed truth. Promotion of results happens via git merge in RegistryService (K5c), never by this operator writing weights.
        properties:
          spec:
            description: |-
              Desired state of a [`TrainingRun`].

              # Validation
              - `modelRef` must be non-empty.
              - `datasetMount` must be an absolute path.
            properties:
              adapterName:
                description: |-
                  Name of the output file-based adapter to produce, e.g.
                  `00_finetune.safetensors`. Defaults are chosen by the reconciler when
                  omitted.
                nullable: true
                type: string
              datasetMount:
                description: |-
                  Absolute mount path where the training dataset is delivered to the job
                  (a hyprstream VFS mount, K4b/CSI).
                type: string
              modelRef:
                description: Name of the [`crate::models::Model`] (same namespace) to train against.
                type: string
              resources:
                description: Coarse resource request for the training pod.
                nullable: true
                properties:
                  cpu:
                    description: CPU request in Kubernetes quantity form, e.g. `"2"` or `"500m"`.
                    nullable: true
                    type: string
                  gpus:
                    description: Number of GPUs requested (0 = CPU-only).
                    format: uint32
                    minimum: 0.0
                    nullable: true
                    type: integer
                  memory:
                    description: Memory request in Kubernetes quantity form, e.g. `"8Gi"`.
                    nullable: true
                    type: string
                type: object
              runsOn:
                description: |-
                  Placement class / scheduling hint (`runs_on`) resolved by the scheduling
                  substrate. Free-form label consumed by K4c.
                nullable: true
                type: string
            required:
            - datasetMount
            - modelRef
            type: object
          status:
            description: Observed truth for a [`TrainingRun`]. Written only by the operator.
            nullable: true
            properties:
              message:
                description: Human-readable detail for the current phase.
                nullable: true
                type: string
              observedGeneration:
                description: '`metadata.generation` last reconciled.'
                format: int64
                nullable: true
                type: integer
              phase:
                description: Coarse lifecycle phase (`Pending`, `Running`, `Succeeded`, `Failed`).
                nullable: true
                type: string
              producedAdapter:
                description: Adapter file observed as produced by the run, once complete.
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: TrainingRun
        type: object
        x-kubernetes-validations:
        - rule: self.spec.modelRef != ''
        - rule: self.spec.datasetMount.startsWith('/')
    served: true
    storage: true
    subresources:
      status: {}
