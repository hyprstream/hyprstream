# Generated by `cargo run -p hyprstream-k8s --bin gen-crds`.
# Do not edit by hand; edit the Rust types in crates/hyprstream-k8s/src/.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: models.models.hyprstream.io
spec:
  group: models.hyprstream.io
  names:
    categories:
    - hyprstream
    kind: Model
    plural: models
    shortNames:
    - hsmodel
    singular: model
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repo
      name: Repo
      type: string
    - jsonPath: .status.observedRef
      name: Ref
      type: string
    - jsonPath: .spec.stage
      name: Stage
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A hyprstream model as a git-ref intent (Stage → Train → Evaluate → Promote). spec is desired intent; status is observed git truth. The operator never writes weights.
        properties:
          spec:
            description: |-
              Desired state of a [`Model`]: a git-ref intent.

              # Validation
              - `repo` must be non-empty.
              - `gitRef` (when set) must be non-empty.
            properties:
              gitRef:
                description: |-
                  Git ref (branch, tag, or commit) to observe. Defaults to the repository
                  default branch when omitted.
                nullable: true
                type: string
              repo:
                description: Git repository the model lives in (URL or registry id). Required.
                type: string
              stage:
                default: Staged
                description: STEP stage the operator should observe this model at.
                enum:
                - Staged
                - Promoted
                type: string
            required:
            - repo
            type: object
          status:
            description: Observed git/runtime truth for a [`Model`]. Written only by the operator.
            nullable: true
            properties:
              message:
                description: Human-readable detail for the current phase.
                nullable: true
                type: string
              observedGeneration:
                description: '`metadata.generation` last reconciled, for staleness detection.'
                format: int64
                nullable: true
                type: integer
              observedRef:
                description: Resolved commit (or ref) currently observed in git.
                nullable: true
                type: string
              phase:
                description: Coarse lifecycle phase (`Pending`, `Ready`, `Failed`, ...).
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: Model
        type: object
        x-kubernetes-validations:
        - rule: self.spec.repo != ''
        - rule: '!has(self.spec.gitRef) || self.spec.gitRef != '''''
    served: true
    storage: true
    subresources:
      status: {}
