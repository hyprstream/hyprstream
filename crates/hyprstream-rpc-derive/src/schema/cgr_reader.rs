//! CGR-based Cap'n Proto schema parser.
//!
//! Re-exported from `hyprstream_rpc_build::schema::cgr_reader` — the canonical
//! implementation lives in the build crate so it can be shared between the
//! proc-macro and the TypeScript codegen binary.

pub use hyprstream_rpc_build::schema::cgr_reader::*;
