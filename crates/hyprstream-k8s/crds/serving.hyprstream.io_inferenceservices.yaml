# Generated by `cargo run -p hyprstream-k8s --bin gen-crds`.
# Do not edit by hand; edit the Rust types in crates/hyprstream-k8s/src/.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: inferenceservices.serving.hyprstream.io
spec:
  group: serving.hyprstream.io
  names:
    categories:
    - hyprstream
    kind: InferenceService
    plural: inferenceservices
    shortNames:
    - hsisvc
    singular: inferenceservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.model
      name: Model
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.url
      name: URL
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Serving intent for a model:branch with autoscaling bounds and a statefulness class. Subspaced under serving.hyprstream.io so it never collides with serving.kserve.io.
        properties:
          spec:
            description: |-
              Desired state of an [`InferenceService`].

              # Validation
              - `model` must be non-empty.
              - `minReplicas` must be `<= maxReplicas`.
              - `maxReplicas` must be `>= 1`.
            properties:
              maxReplicas:
                default: 1
                description: Maximum replica count. Must be at least `1`.
                format: uint32
                minimum: 0.0
                type: integer
              minReplicas:
                default: 0
                description: Minimum replica count. `0` permits scale-to-zero for stateless services.
                format: uint32
                minimum: 0.0
                type: integer
              model:
                description: |-
                  Model to serve, in `model:branch` form (branch optional, defaults to the
                  promoted ref).
                type: string
              statefulness:
                default: TttStateful
                description: Statefulness class driving autoscaling behaviour.
                enum:
                - Stateless
                - TttStateful
                type: string
            required:
            - model
            type: object
          status:
            description: |-
              Observed serving truth for an [`InferenceService`]. Written only by the
              operator.
            nullable: true
            properties:
              message:
                description: Human-readable reconcile outcome or reason for the current phase.
                nullable: true
                type: string
              observedGeneration:
                description: '`metadata.generation` last reconciled.'
                format: int64
                nullable: true
                type: integer
              phase:
                description: Coarse lifecycle phase (`Pending`, `Ready`, `Failed`, ...).
                nullable: true
                type: string
              readyReplicas:
                description: Number of ready replicas currently serving.
                format: uint32
                minimum: 0.0
                nullable: true
                type: integer
              url:
                description: Externally reachable URL, once serving.
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: InferenceService
        type: object
        x-kubernetes-validations:
        - rule: self.spec.model != ''
        - rule: self.spec.minReplicas <= self.spec.maxReplicas
        - rule: self.spec.maxReplicas >= 1
    served: true
    storage: true
    subresources:
      status: {}
