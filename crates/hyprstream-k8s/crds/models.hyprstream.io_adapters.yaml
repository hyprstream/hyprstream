# Generated by `cargo run -p hyprstream-k8s --bin gen-crds`.
# Do not edit by hand; edit the Rust types in crates/hyprstream-k8s/src/.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: adapters.models.hyprstream.io
spec:
  group: models.hyprstream.io
  names:
    categories:
    - hyprstream
    kind: Adapter
    plural: adapters
    shortNames:
    - hsadapter
    singular: adapter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelRef
      name: Model
      type: string
    - jsonPath: .spec.file
      name: File
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A file-based LoRA adapter intent stored as adapters/NN_name.safetensors on a model. Adapters are files, not branches.
        properties:
          spec:
            description: |-
              Desired state of an [`Adapter`]: a file-based adapter intent.

              # Validation
              - `modelRef` must be non-empty.
              - `file` must match the file-based adapter naming convention
                `NN_name.safetensors` (two leading digits, then `_`, then a name,
                ending in `.safetensors`).
            properties:
              baseRef:
                description: Optional base git ref the adapter was trained against, for provenance.
                nullable: true
                type: string
              file:
                description: Adapter filename under `adapters/`, e.g. `00_style.safetensors`.
                type: string
              modelRef:
                description: Name of the [`Model`] (same namespace) this adapter attaches to.
                type: string
            required:
            - file
            - modelRef
            type: object
          status:
            description: Observed truth for an [`Adapter`]. Written only by the operator.
            nullable: true
            properties:
              observedFile:
                description: The adapter file the operator observed on the model.
                nullable: true
                type: string
              observedGeneration:
                description: '`metadata.generation` last reconciled.'
                format: int64
                nullable: true
                type: integer
              phase:
                description: Coarse lifecycle phase (`Pending`, `Ready`, `Failed`, ...).
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: Adapter
        type: object
        x-kubernetes-validations:
        - rule: self.spec.modelRef != ''
        - rule: self.spec.file.matches('^[0-9]{2}_[A-Za-z0-9._-]+\\.safetensors$')
    served: true
    storage: true
    subresources:
      status: {}
